{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {   
            "label": "cmake",
            "type": "shell",
            "command": "cmake -S skipListLab -B skipLabBuild",
        },
        {
            "label": "build",
            "type":"shell",
            "command":"cmake --build skipLabBuild",
            "dependsOn":"cmake"
        }
    ]
}